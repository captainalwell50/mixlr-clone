# Deprecated — use Flutter Studio Desktop

**Do not use this Electron shell for product.**

The premium Studio desktop app lives in:

[`../studio_desktop/`](../studio_desktop/)

```bash
cd studio_desktop
flutter run -d macos --dart-define=API_BASE=https://soundmix.live
```
